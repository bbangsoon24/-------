✅ 자바스크립트 자료형별 핵심 정리

1. 숫자 (Number)

👉 계산용 (합계, 평균, 퍼센트, 가격)

연산자 사용: + - * / %

결과 = 숫자

let a = 10, b = 3;
console.log(a + b); // 13
console.log(a / b); // 3.333...

2. 문자열 (String)

👉 글자 데이터 (이름, 메시지, 설명)

연결 연산: +

결과 = 문자열

let name = "홍길동";
let msg = "안녕하세요, " + name + "님!";
console.log(msg); // "안녕하세요, 홍길동님!"

3. 불리언 (Boolean)

👉 조건 분기 (참/거짓)

비교 연산(===, >, <)이나 논리 연산(&&, ||, !) → 무조건 true / false

let age = 20;
console.log(age >= 18); // true
console.log(age < 18);  // false

4. 배열 (Array)

👉 목록 관리 (여러 값 저장)

인덱스(arr[0])로 접근

길이: .length

반복문과 자주 사용

let scores = [80, 90, 70];
console.log(scores[0]);    // 80
console.log(scores.length); // 3

5. 객체 (Object)

👉 정보 묶음 (한 대상을 속성으로 표현)

키(key):값(value) 쌍

점(.)이나 대괄호([])로 접근

let user = { name: "홍길동", age: 25, isMember: true };
console.log(user.name);  // "홍길동"
console.log(user["age"]); // 25

6. null / undefined

👉 값 없음 표현

null: 일부러 “없다”라고 지정

undefined: 값이 아직 정해지지 않음

let coupon = null;   // 쿠폰 없음
let discount;        // undefined (값 미지정)